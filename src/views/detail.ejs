<!DOCTYPE html>
<html>
  <head>
    <title>Detail: <%= sync.title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="p-4">
    <div class="container" style="max-width: 960px">
      <a href="/" class="btn btn-sm btn-secondary mb-3">← Back</a>
      <h1 class="mb-4">📄 Detail for "<%= sync.title %>"</h1>

      <div class="mb-3">
        <strong>Target Table:</strong> <%= sync.target_table %><br />
        <strong>Total Rows:</strong> <%= count %><br />
        <strong>Estimated Size:</strong> <%= size_readable %>
      </div>

      <h4>🧱 Table Structure</h4>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Column Name</th>
            <th>Data Type</th>
          </tr>
        </thead>
        <tbody>
          <% columns.forEach(col => { %>
          <tr>
            <td><%= col.name %></td>
            <td><%= col.type %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </body>
</html>
